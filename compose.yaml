services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-selamate}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-selamate_dev}
      POSTGRES_DB: ${POSTGRES_DB:-selamate}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - selamate_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 10
volumes:
  selamate_postgres:
