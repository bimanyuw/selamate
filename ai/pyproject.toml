[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "selamate-ai"
version = "0.1.0"
description = "Fondasi pipeline AI untuk Selamate EWS"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
